{
  "name": "next-evento-painel",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs"
  },
  "dependencies": {
    "mongodb": "^6.0.0"
  }
}
